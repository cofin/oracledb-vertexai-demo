# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
!resources/lib
!app/lib
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
.pdm-python
.envs/
# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
.python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# PEP 582; used by e.g. github.com/David-OConnor/pyflow
__pypackages__/

# celery beat schedule file
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# dotenv
.env
.env.*
env.bak/
service_account.json

# virtualenv
.venv
venv/
ENV/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# IDE settings
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
.idea/
!.vscode/settings.json
!.vscode/launch.json
# Version file generated by hatch-vcs
app/domain/web/public
.ruff_cache


# App specifics
.env
*.log
log/*
*.csv
tmp/*
node_modules/
.astro
.vite
.npmrc
uv.toml
# Vite build output served by litestar-vite (bundle dir lives inside the web domain).
# See src/app/domain/web/__init__.py — `dist/` holds manifest.json + hashed bundles
# emitted by `manage.py assets build`; `dist/hot` is the HMR marker written in dev mode.
src/app/domain/web/static/dist/
# Generated client SDK (re-emitted by `manage.py assets generate-types`)
src/resources/generated/
demo.py
.tmp
todo

# Wallet files and environment-specific configs
.envs/
!tools/lib
!src/app/lib
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads
